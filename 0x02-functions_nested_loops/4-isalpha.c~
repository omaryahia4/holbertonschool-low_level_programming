#include "holberton.h"
/**
* _isalpha - isalpha function.
*
* Return: 1 if c is a letter. 0 if not.
*/
int _isalpha(int c)
{
if ((c >= 97 && c <= 122) || (c >= 65 && c <= 90))
{
return (1);
}
else
{
return (0);
}
_putchar('\n');
}
